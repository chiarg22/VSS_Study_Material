// const addNum = (a, b) => {
//     const result = a + b;
//     return result;
//   };
  
//   console.log("Result", addNum(10, 9));

// input - getGreetings("sushmitha")
// output - Hello Sushmitha, Welcome to the team.

  function getGreetings(name="user")
  { 
    if(name!== null || type of name!== "string")
    return("Please enter a valid entry");
else(name[0].toUpperCase && name.slice[1].tolowercase)
const compname= name[0].toUpperCase + name.slice[1].tolowercase();
return("Hello" + compname + "Welcome to the team.");
    else
    return("Hello User, Welcome to the team.");
  }
  console.log(getGreetings(sushmitha));
