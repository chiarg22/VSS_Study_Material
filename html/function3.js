function multiply(a,b){
    return a*b;
}

console.log(multiply("x",2));

//NAN is the output
//NAN+NAN or NAN-NAN=NAN   

//const addNums = (a, b) => {
    //console.log("a,b", a, b);
    //return a + b;
  //}; The output will be undefined undefined as there is no default value has been given to the user and also not on runtime.
  